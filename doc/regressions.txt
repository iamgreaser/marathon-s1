Regressions known at this stage:

- The LAB1 life monitor always shows as a life monitor.
   - UPDATE: It's now a borked-looking permanent ring monitor.
- Left-entry-only and right-entry-only doors are weirdly offset to the left.
   - This seems to have "fixed itself"...? Keep an eye on this one.
- The one remaining corridor that actually needs to teleport does not work.
   - SKY2 corridor does not work. This would be a nice way to move everything over to the left to make room for more levels...
- The chunksave mechanism is still broken (it was working at one point, honest!), although at least some rings get collected now.
   - I had to force Sonic to deactivate when off-screen in order to not break teleporters due to checking physics against the wrong chunk layout. I need to confirm if that fixes it.
      - UPDATE: No, it is not fixed.
- SKY3: The button Robotnik jumps on isn't drawn. I'm not sure how to do this one relatively yet, as the logic is in Robotnik's code.
- SKY3: Sonic can't teleport out (as per all teleporters being broken).
- SKY2: The spike wall seems to use an absolute position to work out when to move... and, well, it doesn't move.
- Transitions across different tilesets are still a bit jank as the tilemaps and art and palettes get replaced, but at least chunks have consistent physics now!
- SKY1 to SKY2 transition can result in being bombarded with a non-stop lightning flash.
- SKY3: The background tiles get mangled when the electric bolts happen.
- The invincibility timer (and some other timers likely) only ticks down when Sonic is on-screen or close to it.

Stuff which will result in a regression if I'm not careful:

- Rings with pixel X>=$FF00 don't disappear from the screen when collected. (This used to be X>=$8000.)
